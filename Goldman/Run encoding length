string encode(string src)
{
string ans="";
    int count=1;
    for(int i=0;i<src.length();i++)
    {
        if(src[i]==src[i+1])
            count++;
        else
        {
            ans+=src[i];
            ans+=to_string(count);
            count=1;
        }
    }
    return ans;
}
