class Solution{
public:
    int minSteps(int D)
    {
        D=abs(D);
    	int sum=0,count=0;
	    while (sum<D || (sum-D)%2!=0)
        {   
		    count++;
		    sum+=count;
	    }
        return count;
    }
};
